/**
 * Re-export ID generation utilities from @accomplish/shared.
 *
 * This file re-exports the ID utilities from the shared package so that
 * code importing from @accomplish/core can also access these functions.
 */
export { createTaskId, createMessageId } from '@accomplish/shared';
